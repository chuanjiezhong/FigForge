# Pipeline 结果解读稿片段（中文）

> 来源：结合 `docs/文章模版/3.docx` 中 **Methods（limma + pheatmap 热图）** 与 **Results（Figure 1 热图图注）** 的写法，抽象为**与疾病无关**的模版句。  
> 占位符请按本次运行替换：`{对比组A}`、`{对比组B}`、`{物种/组织}`、`{热图文件相对路径}`、`{火山图路径}`、`{limma阈值描述}` 等。

---

## Methods（方法）— 差异分析与热图

**差异表达分析**：采用 **limma** 对经 log2 转换并标准化后的表达矩阵进行线性建模，比较 **{对比组A}** 与 **{对比组B}**。差异基因判定标准为 **{limma阈值描述，例如：|log2FC| > 1 且 FDR < 0.05}**（与本次分析脚本参数一致）。

**热图**：使用 **pheatmap**（R）基于上述差异结果选取 **Top 上调 / Top 下调** 基因（或用户指定的感兴趣基因列表），绘制表达热图。**行**为基因，**列**为样本；表达量经 **{行标准化方式，如：按行 z-score 标准化}** 后着色，色阶表示相对表达高低（蓝—白—红或本次输出图例所示配色）。**{若启用列/行聚类：样本侧/基因侧层次聚类用于展示表达模式相似性；若未启用：样本/基因顺序按分析设定固定。}**

---

## Results（结果）— 热图段落示例

**热图（差异基因表达谱）**：与 **{对比组A}** 相比，**{对比组B}** 中筛选得到 **{数量或描述}** 个差异表达基因。热图（见 **`{热图文件相对路径}`**）展示了其中 **代表性差异基因** 在各类样本中的表达分布：颜色深浅反映经标准化后的相对表达水平；整体上，上调基因在 **{对比组B}** 中呈 **{颜色方向}** 趋势，下调基因呈 **{相反方向}** 趋势，与分组设计一致。

**（可选，与火山图联动）**：上述基因集合与火山图（**`{火山图路径}`**）中的显著点相对应；热图强调 **样本维度上的共表达模式**，火山图强调 **单基因的效应量与显著性**。

---

## 讨论提示（一句占位）

热图结果需在正文中与 **对比组定义**、**标准化与阈值** 保持一致；若后续补充通路分析（如 GO/KEGG）或 WGCNA，可在讨论中引用 **同一基因列表** 的生物学意义（模版论文 `3.docx` 中采用「差异 → 富集 → 模块 → Hub 基因」的延长线，可按实际分析删减）。
